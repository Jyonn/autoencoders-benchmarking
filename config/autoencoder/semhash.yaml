name: semhash
kind: autoencoder
fit: true
output_representation: ${output_representation:latents}$
batch_size: ${transform_batch_size:512}$

model:
  name: semhash
  config:
    latent_dim: ${latent_dim:32}$
    binarization: ${binarization:ste}$
    binarization_weight: ${binarization_weight:0.1}$
    balance_weight: ${balance_weight:0.01}$
    decorrelation_weight: ${decorrelation_weight:0.01}$

encoder:
  name: mlp
  config:
    hidden_dims: ${encoder_hidden_dims:256,128,64}$
    activation: ${activation:relu}$
    use_bias: ${use_bias:true}$

trainer:
  epochs: ${epochs:0}$
  patience: ${patience:5}$
  batch_size: ${train_batch_size:256}$
  learning_rate: ${learning_rate:0.001}$
  optimizer_name: ${optimizer_name:adamw}$
  weight_decay: ${weight_decay:0.0}$
  validation_ratio: ${validation_ratio:0.1}$
  test_ratio: ${test_ratio:0.1}$
  device: ${train_device:cpu}$
  seed: ${seed:42}$
